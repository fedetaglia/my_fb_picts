  <div class="row">
    <h3>
    <%= link_to "Sign in with Facebook", user_omniauth_authorize_path(:facebook), {class: 'btn btn-primary' } %>
    </h3>
  </div>




<h4>photos index</h4>


<% @photos.each do |photo|  %>

<div class="photo">
  <img src="<%= photo['images'].last['source'] %>" alt="" value="<%= photo['id'] %>" >
  <p><%= photo['name'] %> </p>
</div>


<% end %>